---
import AppLayout from '@/layouts/AppLayout.astro';

const {
    title
} = Astro.props;
---

<AppLayout title={title} mainCls={"container min-h-[calc(100dvh-4rem)] flex items-center flex-col justify-center"}>
    <slot />
</AppLayout>
