---
import AppLayout from '@/layouts/AppLayout.astro';

const {
    title
} = Astro.props;
---

<AppLayout title={title} mainCls={"container min-h-[calc(100vh-4rem)] flex items-center justify-center"}>
    <slot />
</AppLayout>
