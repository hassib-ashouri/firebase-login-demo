---
import AppLayout from '@/layouts/AppLayout.astro';

const {
    title,
    isUserAuthenticated,
} = Astro.props;
---

<AppLayout
    title={title}
    isUserAuthenticated={isUserAuthenticated}
    mainCls={"container min-h-[calc(100dvh-4rem)] flex items-center flex-col justify-center"}>
    <slot />
</AppLayout>
